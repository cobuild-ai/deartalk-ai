# ==============================================================================
# DearTalk AI: Keystore Properties Template
# ==============================================================================
# Copy this file to `keystore.properties` and fill in your signing credentials.
# WARNING: NEVER commit `keystore.properties` or `*.keystore` files to Git!
# ==============================================================================

RELEASE_STORE_FILE=../secrets/deartalk.keystore
RELEASE_STORE_PASSWORD=your_store_password
RELEASE_KEY_ALIAS=your_key_alias
RELEASE_KEY_PASSWORD=your_key_password
